@extends('layouts.admin')
@section('title','Reviews')
@section('content')<div class="table-card"><table class="table"><tr><th>Product</th><th>Customer</th><th>Rating</th><th>Comment</th><th>Approved</th><th></th></tr>@foreach($reviews as $review)<tr><td>{{ $review->product->name }}</td><td>{{ $review->user->name }}</td><td>{{ $review->rating }}</td><td>{{ $review->comment }}</td><td>{{ $review->is_approved?'Yes':'No' }}</td><td><form class="d-inline" method="post" action="{{ route('admin.reviews.update',$review) }}">@csrf @method('PUT')<input type="hidden" name="is_approved" value="{{ $review->is_approved?0:1 }}"><button class="btn btn-sm btn-outline-primary">{{ $review->is_approved?'Hide':'Approve' }}</button></form><form class="d-inline" method="post" action="{{ route('admin.reviews.destroy',$review) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@endforeach</table>{{ $reviews->links() }}</div>@endsection
