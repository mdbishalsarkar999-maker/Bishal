@extends('layouts.admin')
@section('title','Inventory')
@section('content')<div class="table-card"><table class="table align-middle"><tr><th>Product</th><th>Stock</th><th>Alert</th><th>Update</th></tr>@foreach($products as $product)<tr><td>{{ $product->name }}</td><td>{{ $product->stock_quantity }}</td><td>@if($product->stock_quantity <= $product->low_stock_limit)<span class="badge text-bg-danger">Low stock</span>@else<span class="badge text-bg-success">OK</span>@endif</td><td><form class="d-flex gap-2" method="post" action="{{ route('admin.inventory.update',$product) }}">@csrf<input class="form-control" style="max-width:120px" name="stock_quantity" value="{{ $product->stock_quantity }}"><input class="form-control" name="note" placeholder="Note"><button class="btn btn-sm btn-primary">Update</button></form></td></tr>@endforeach</table>{{ $products->links() }}</div>@endsection
