@extends('layouts.admin')
@section('title','Coupons')
@section('content')<div class="table-card"><a class="btn btn-primary mb-3" href="{{ route('admin.coupons.create') }}">Add Coupon</a><table class="table"><tr><th>Code</th><th>Type</th><th>Value</th><th>Expiry</th><th>Status</th><th></th></tr>@foreach($coupons as $coupon)<tr><td>{{ $coupon->code }}</td><td>{{ $coupon->discount_type }}</td><td>{{ $coupon->discount_value }}</td><td>{{ $coupon->expires_at->format('d M Y') }}</td><td>{{ $coupon->is_active?'Active':'Inactive' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.coupons.edit',$coupon) }}">Edit</a><form class="d-inline" method="post" action="{{ route('admin.coupons.destroy',$coupon) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@endforeach</table>{{ $coupons->links() }}</div>@endsection
