@extends('layouts.admin')
@section('title',$coupon->exists?'Edit Coupon':'Add Coupon')
@section('content')<form class="table-card" method="post" action="{{ $coupon->exists ? route('admin.coupons.update',$coupon) : route('admin.coupons.store') }}">@csrf @if($coupon->exists)@method('PUT')@endif<input class="form-control mb-3" name="code" placeholder="Code" value="{{ old('code',$coupon->code) }}"><select class="form-select mb-3" name="discount_type"><option value="percentage" @selected(old('discount_type',$coupon->discount_type)=='percentage')>Percentage</option><option value="fixed" @selected(old('discount_type',$coupon->discount_type)=='fixed')>Fixed</option></select><input class="form-control mb-3" name="discount_value" placeholder="Discount value" value="{{ old('discount_value',$coupon->discount_value) }}"><input class="form-control mb-3" name="expires_at" type="date" value="{{ old('expires_at',optional($coupon->expires_at)->format('Y-m-d')) }}"><label class="form-check mb-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$coupon->is_active ?? true))> Active</label><button class="btn btn-primary">Save</button></form>@endsection
