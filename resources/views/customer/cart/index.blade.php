@extends('layouts.app')
@section('title','Cart')
@section('content')<div class="container py-5"><h1>Cart</h1><div class="table-card"><table class="table align-middle"><thead><tr><th>Product</th><th>Price</th><th width="160">Quantity</th><th>Total</th><th></th></tr></thead><tbody>@php($subtotal=0)@forelse($cart->items as $item)@php($line=$item->quantity*$item->product->selling_price)@php($subtotal+=$line)<tr><td>{{ $item->product->name }}</td><td>৳{{ number_format($item->product->selling_price,2) }}</td><td><form method="post" action="{{ route('cart.update',$item) }}">@csrf @method('PUT')<input class="form-control" name="quantity" type="number" min="1" value="{{ $item->quantity }}" onchange="this.form.submit()"></form></td><td>৳{{ number_format($line,2) }}</td><td><form method="post" action="{{ route('cart.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-muted">Your cart is empty.</td></tr>@endforelse</tbody></table><div class="d-flex justify-content-between align-items-center"><strong>Subtotal: ৳{{ number_format($subtotal,2) }}</strong><a class="btn btn-primary" href="{{ route('checkout.create') }}">Checkout</a></div></div></div>@endsection
