@extends('layouts.app')
@section('title','Order Details')
@section('content')<div class="container py-5"><h1>Order {{ $order->order_number }}</h1><div class="row g-4"><div class="col-lg-8"><div class="table-card"><table class="table"><thead><tr><th>Product</th><th>Qty</th><th>Price</th><th>Total</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->quantity }}</td><td>৳{{ number_format($item->unit_price,2) }}</td><td>৳{{ number_format($item->total_price,2) }}</td></tr>@endforeach</tbody></table></div></div><div class="col-lg-4"><div class="table-card"><p>Status: <span class="badge text-bg-info">{{ $order->status }}</span></p><p>Payment: {{ str_replace('_',' ',$order->payment->method) }} / {{ $order->payment->status }}</p><p>Delivery: ৳{{ number_format($order->delivery_charge,2) }}</p><p>Discount: ৳{{ number_format($order->discount_amount,2) }}</p><h4>Total: ৳{{ number_format($order->total_amount,2) }}</h4>@if(in_array($order->status,['pending','confirmed','processing']))<form method="post" action="{{ route('orders.cancel',$order) }}">@csrf<button class="btn btn-outline-danger">Cancel Order</button></form>@endif</div></div></div></div>@endsection
